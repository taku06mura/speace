= form_for @team do |f|
  = f.text_field :name, class: "chat-team-form__input", placeholder: "チーム名を入力してください"
  = f.collection_check_boxes :user_ids, User.all, :id, :name
  = f.submit "Save", class: "chat-team-form__action-btn"